const data = ' en promoción';
const text = 'Tv de 20 pulgadas';

// Repetir una cadena de texto
console.log(`${text} ${data.repeat(3)} !!!`);

let frutas = 'Manzana, Pera, Uva, Plátano';

console.log(frutas.split(', '));
