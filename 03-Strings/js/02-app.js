const producto = "Monitor 20 Pulgadas"

// Conocer la cantidad de letras de una cadena
console.log(producto.length)

// Encontrar la posicion de una letra o palabra
console.log(producto.indexOf("Monitor")) // -1 es que no lo encontro

// es similar a indexOf pero mejor
console.log(producto.includes("Monitor"))
