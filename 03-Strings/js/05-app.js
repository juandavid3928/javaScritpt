const textData = 'Tv de 20 pulgadas';

// Reemplazar una palabra
console.log(textData.replace('pulgadas', '"'));
console.log(textData.replace('Tv', 'Monitor Curvo'));

// Contar o mostrar cierta parte de una frase
console.log(textData.slice(0, 9));
console.log(textData.substring(8, 0));


const user = 'Juan cochero';
console.log(user.charAt(0));
