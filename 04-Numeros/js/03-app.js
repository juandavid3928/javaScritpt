//* Redondea el número siempre al hacia abajo
console.log(`Floor: Original: 2.9, Redondiar siempre hacia abajo con floor: ${Math.floor(2.9)}`)

//* Redondea hacia arriba
console.log(`Ceil: Original: 2.1, Redondiar siempre hacia arriba con ceil: ${Math.ceil(2.1)}`);

//* Redondear
console.log(`Round: Original: 2.5, Redondiar: ${Math.round(2.5)}`);

//* Numero aleatorio
console.log(`Random da un numero aleatorio entre 0 y 1: ${Math.random().toFixed(0)}`);

//* Devuelve el mas alto
console.log(`Max devuelve el mas alto de: 3, 4, 5, 7, 9, 2, 8, 1, el mas alto es: ${Math.max(3, 4, 5, 7, 9, 2, 8, 1)}`);

//* Devuelve el mas bajo
console.log(`Min devuelve el mas bajo de: 3, 4, 5, 7, 9, 2, 8, 1, el mas bajo es: ${Math.min(3, 4, 5, 7, 9, 2, 8, 1)}`);

//* la potencia
console.log(`Potencia de 2 a las 3 es: ${Math.pow(2, 3)}`);

//* Calcula la raíz cuadrada
console.log(`Raiz cuadrada de 9 es: ${Math.sqrt(9)}`);

//* Crear un numero random
console.log(`Crear un numero aleatorio: ${Math.floor(Math.random() * (100 - 1)) + 1}`);