// * Iterator For Loop


for (let i = 1; i < 5; i += 2) {
    console.log(`Numero: ${i}`);
}

for (let i = 1; i < 11; i++) {
    i % 2 === 0 ? console.log(`El numero ${i} es par`) : console.log(`El numero ${i} no es par`);
}
