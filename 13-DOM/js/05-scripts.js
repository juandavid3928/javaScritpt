// Query all
const card = document.querySelectorAll(".card")
console.log(card)

const formularios = document.querySelectorAll(".formularios")
console.log(formularios)

// Si un elemento no existe, retorna  no list